# 🧪 Tests Directory

تست‌های سیستم

## 📁 فایل‌ها

```
tests/
├── test_system.py    # تست جامع سیستم
└── README.md         # این فایل
```

---

## 🚀 استفاده

### تست جامع سیستم

```bash
# اجرا در Docker
docker exec app_backend python3 /app/tests/test_system.py
```

**تست‌های انجام شده:**
- ✅ آپلود فایل به MinIO
- ✅ Query عادی به RAG Core
- ✅ Streaming query به RAG Core

---

## 🗄️ Management Commands

### پاکسازی دیتابیس

```bash
# حذف کاربران غیر سوپرادمین و داده‌های مرتبط
docker exec app_backend python3 manage.py cleanup_database --confirm
```

این دستور موارد زیر را حذف می‌کند:
- تمام کاربران غیر سوپرادمین
- تمام مکالمات و پیام‌ها
- اعلان‌ها و نوتیفیکیشن‌های کاربران
- اشتراک‌ها و تراکنش‌ها (نه پلن‌ها)
- تیکت‌های پشتیبانی
- نشست‌ها و لاگ‌های حسابرسی
- سازمان‌ها و دعوت‌نامه‌ها
- آنالیتیکس کاربران

**توجه:** پلن‌های اشتراک حذف نمی‌شوند و به عنوان تنظیمات اولیه سیستم باقی می‌مانند.

---

## 📝 نکات

- قبل از commit تست‌ها را اجرا کنید
- مطمئن شوید سرویس‌های خارجی (MinIO, RAG Core) در دسترس هستند
- برای دیباگ از لاگ‌های Docker استفاده کنید
- **پاکسازی فایل‌های موقت MinIO:** سیستم مرکزی (RAG Core) خودش این کار را به صورت دوره‌ای انجام می‌دهد
